name: langgraph_client
description: A Dart client for the LangGraph API that enables applications to interact with LangGraph services, threads, assistants, runs, and crons with support for streaming operations.
repository: https://github.com/EsotericTech/langgraph-client
publish_to: none
version: 1.0.0

environment:
  sdk: ^3.8.0

dependencies:
  json_annotation: ^4.9.0
  equatable: ^2.0.7
  sse_stream:
    path: /Users/tilapia/TilapiaFiles/Projects/FlutterPackage/sse_stream
  http: ^1.3.0

dev_dependencies:
  lints: ^5.1.1
  test: ^1.24.0
  build_runner: ^2.4.13
  json_serializable: ^6.9.0
  mockito: ^5.4.4
